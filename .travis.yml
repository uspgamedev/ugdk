language: cpp

compiler:
  - gcc
  - clang

before_script:
  - sudo apt-get update
  - sudo apt-get install swig2.0 liblua5.1-0-dev python-dev libpng12-dev 

script:
  - cmake .
  - make ugdk_test && make ugdk_script_test
  - ./test/ugdk_test

notifications:
  email:
      - travis@uspgamedev.org
